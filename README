# ================== REMARK =====================================
# if you do not have a full write access to github, 
# make copy of the file "repositories" as "repositories.writable",
# and keep inside only those repositories you can push into

# nemesis-build
utilites for Nemesis compilation
